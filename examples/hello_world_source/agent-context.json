{
  "static": {
    "project_name": "hello_world",
    "description": "A minimal Flask REST API that provides personalised greetings, records them in memory, and exposes health-check and listing endpoints.",
    "primary_language": "Python",
    "runtime_version": ">=3.11",
    "frameworks": [
      "Flask",
      "pytest"
    ],
    "entry_point": "app.py",
    "test_command": "pytest",
    "restricted_write_paths": [],
    "environment_variables": [],
    "domain_concepts": [
      "Greeting: A recorded personalised message containing a user's name and a hello message, stored in the in-memory list.",
      "Health Check: A lightweight endpoint (/health) that returns an 'ok' status to confirm the service is running.",
      "Service Root: The index endpoint (/) that returns the service name and version metadata."
    ]
  },
  "dynamic": {
    "last_scanned": "2026-04-29T10:58:13.258933+00:00",
    "secondary_languages": [],
    "build_system": "pip",
    "build_command": "pip install -e '.[dev]' 2>/dev/null || pip install -r requirements.txt",
    "install_command": "pip install -r requirements.txt",
    "run_command": "python app.py",
    "test_framework": "pytest",
    "test_directory": "tests",
    "source_directories": [
      "."
    ],
    "module_layout": {
      "app": "app.py",
      "tests": "tests/"
    },
    "architecture_summary": "A single-module Flask application that exposes four REST endpoints (index, health, greet, and greetings). All state is held in an in-memory list within the app module, with no external database or persistence layer. Tests exercise the API through Flask's test client.",
    "key_components": [
      {
        "name": "app",
        "path": "app.py",
        "responsibility": "Defines the Flask application, all route handlers, and the in-memory greetings store."
      },
      {
        "name": "tests",
        "path": "tests/test_app.py",
        "responsibility": "Contains API-level tests for the Flask application endpoints."
      }
    ],
    "agent_safe_operations": [
      "Adding new GET/POST endpoints in app.py",
      "Adding new test files or test functions under tests/",
      "Updating greeting message formatting logic",
      "Adding new utility functions in separate modules",
      "Updating dependencies in requirements.txt"
    ],
    "agent_forbidden_operations": [
      "Removing or renaming existing public endpoints (breaks API consumers)",
      "Changing the in-memory _greetings data structure shape without updating all consumers and tests",
      "Changing the default port or host binding without updating documentation",
      "Deleting tests/__init__.py (needed for test discovery)"
    ],
    "potential_pitfalls": [
      "The _greetings list is module-level global state; it persists across requests within a process but resets on restart, and tests may leak state between test functions if not isolated.",
      "Flask's test client must be obtained via app.test_client(); importing app directly gives you the Flask instance, not a running server.",
      "The app uses @app.get() shorthand (Flask 2.x+); older Flask versions will raise AttributeError.",
      "No CORS headers are configured, so browser-based clients hitting the API will be blocked by default.",
      "requirements.txt includes httpx but the app only uses Flask; httpx may be intended for integration tests \u2014 removing it could break test fixtures."
    ],
    "conventions": {
      "naming": "snake_case",
      "structure": "single-package"
    },
    "has_ci": true,
    "has_openapi": false
  }
}